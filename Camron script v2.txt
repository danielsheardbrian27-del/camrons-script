-- =============================================
-- Camron Script - ALWAYS-ON Headshot Aimbot + Full GUI
-- • Aimbot is ALWAYS ACTIVE (no hold key)
-- • STRICT HEADSHOT ONLY
-- • ONLY locks on players INSIDE your FOV radius AND visible on screen
-- • Auto-switches instantly when current target dies (HP ≤ 0)
-- • Full draggable GUI with FOV slider, toggles, and live status
-- • Tracers + info box (Name | HP% | Distance) at end of tracer
-- • Press INSERT to toggle cheat | RightShift to show/hide GUI
-- =============================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- ===================== SETTINGS =====================
local AIMBOT_ENABLED = true
local ESP_ENABLED = true
local FOV = 220
local TOGGLE_CHEAT_KEY = Enum.KeyCode.Insert
local TOGGLE_GUI_KEY = Enum.KeyCode.RightShift

local currentTarget = nil

-- ===================== DRAWINGS =====================
local drawings = {}
local fovCircle = Drawing.new("Circle")
fovCircle.Thickness = 2
fovCircle.Color = Color3.fromRGB(255, 80, 80)
fovCircle.Filled = false
fovCircle.Transparency = 0.7
fovCircle.NumSides = 100
fovCircle.Radius = FOV

local function createESP(plr)
	if drawings[plr] or plr == LocalPlayer then return end
	local tracer = Drawing.new("Line")
	tracer.Thickness = 2
	tracer.Color = Color3.fromRGB(255, 60, 60)
	tracer.Transparency = 1
	
	local box = Drawing.new("Square")
	box.Thickness = 2
	box.Color = Color3.fromRGB(0, 0, 0)
	box.Filled = true
	box.Transparency = 0.45
	
	local nameText = Drawing.new("Text")
	nameText.Size = 15
	nameText.Color = Color3.fromRGB(255, 255, 255)
	nameText.Center = true
	nameText.Outline = true
	
	local hpText = Drawing.new("Text")
	hpText.Size = 14
	hpText.Color = Color3.fromRGB(0, 255, 0)
	hpText.Center = true
	hpText.Outline = true
	
	local distText = Drawing.new("Text")
	distText.Size = 14
	distText.Color = Color3.fromRGB(255, 255, 255)
	distText.Center = true
	distText.Outline = true
	
	drawings[plr] = {Tracer = tracer, Box = box, Name = nameText, HP = hpText, Distance = distText}
end

local function removeESP(plr)
	if drawings[plr] then
		for _, v in pairs(drawings[plr]) do v:Remove() end
		drawings[plr] = nil
	end
end

for _, p in ipairs(Players:GetPlayers()) do
	createESP(p)
end
Players.PlayerAdded:Connect(createESP)
Players.PlayerRemoving:Connect(removeESP)

-- ===================== AIMBOT HELPERS =====================
local function isValidTarget(plr)
	if not plr or not plr.Character then return false end
	local hum = plr.Character:FindFirstChildOfClass("Humanoid")
	local head = plr.Character:FindFirstChild("Head")
	return hum and hum.Health > 0.1 and head
end

local function getClosestTargetInFOV()
	local bestPlayer = nil
	local bestDist = math.huge
	local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
	
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= LocalPlayer and isValidTarget(plr) then
			local head = plr.Character:FindFirstChild("Head")
			if head then
				local screenPos, onScreen = Camera:WorldToViewportPoint(head.Position)
				if onScreen then
					local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
					if screenDist < FOV and screenDist < bestDist then
						bestDist = screenDist
						bestPlayer = plr
					end
				end
			end
		end
	end
	return bestPlayer
end

-- ===================== FULL GUI =====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CamronScript_GUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 340, 0, 300)
MainFrame.Position = UDim2.new(0.5, -170, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 14)
UICorner.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(255, 60, 60)
UIStroke.Thickness = 2
UIStroke.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "🔴 Camron Script"
Title.TextColor3 = Color3.fromRGB(255, 60, 60)
Title.TextSize = 24
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

-- Draggable
local dragging, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPos = MainFrame.Position
	end
end)
MainFrame.InputChanged:Connect(function(input)
	if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		local delta = input.Position - dragStart
		MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)
UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
end)

-- Toggles
local function createToggle(name, yOffset, default, callback)
	local toggleFrame = Instance.new("Frame")
	toggleFrame.Size = UDim2.new(1, -40, 0, 40)
	toggleFrame.Position = UDim2.new(0, 20, 0, yOffset)
	toggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
	toggleFrame.Parent = MainFrame
	
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 8)
	corner.Parent = toggleFrame
	
	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.65, 0, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextColor3 = Color3.fromRGB(255, 255, 255)
	label.TextSize = 16
	label.Font = Enum.Font.Gotham
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = toggleFrame
	
	local button = Instance.new("TextButton")
	button.Size = UDim2.new(0, 90, 0, 30)
	button.Position = UDim2.new(1, -110, 0.5, -15)
	button.BackgroundColor3 = default and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(170, 0, 0)
	button.Text = default and "ON" or "OFF"
	button.TextColor3 = Color3.fromRGB(255, 255, 255)
	button.Font = Enum.Font.GothamBold
	button.TextSize = 14
	button.Parent = toggleFrame
	
	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 8)
	btnCorner.Parent = button
	
	button.MouseButton1Click:Connect(function()
		default = not default
		button.BackgroundColor3 = default and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(170, 0, 0)
		button.Text = default and "ON" or "OFF"
		callback(default)
	end)
end

createToggle("Always-On Headshot Aimbot", 60, AIMBOT_ENABLED, function(s) AIMBOT_ENABLED = s end)
createToggle("ESP + Tracers + Info Box", 110, ESP_ENABLED, function(s) ESP_ENABLED = s end)

-- FOV Control
local fovLabel = Instance.new("TextLabel")
fovLabel.Size = UDim2.new(1, -40, 0, 30)
fovLabel.Position = UDim2.new(0, 20, 0, 170)
fovLabel.BackgroundTransparency = 1
fovLabel.Text = "FOV Radius: " .. FOV
fovLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
fovLabel.TextSize = 16
fovLabel.Font = Enum.Font.Gotham
fovLabel.Parent = MainFrame

local fovBox = Instance.new("TextBox")
fovBox.Size = UDim2.new(0, 90, 0, 30)
fovBox.Position = UDim2.new(1, -120, 0, 170)
fovBox.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
fovBox.Text = tostring(FOV)
fovBox.TextColor3 = Color3.fromRGB(255, 255, 255)
fovBox.TextSize = 16
fovBox.Font = Enum.Font.Gotham
fovBox.Parent = MainFrame

local fovCorner = Instance.new("UICorner")
fovCorner.CornerRadius = UDim.new(0, 8)
fovCorner.Parent = fovBox

fovBox.FocusLost:Connect(function(enter)
	if enter then
		local num = tonumber(fovBox.Text)
		if num and num >= 30 and num <= 800 then
			FOV = num
			fovLabel.Text = "FOV Radius: " .. FOV
			fovCircle.Radius = FOV
		else
			fovBox.Text = tostring(FOV)
		end
	end
end)

-- Close button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 35, 0, 35)
closeBtn.Position = UDim2.new(1, -45, 0, 8)
closeBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 20
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = MainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 8)
closeCorner.Parent = closeBtn

closeBtn.MouseButton1Click:Connect(function()
	ScreenGui.Enabled = false
	print("Camron Script GUI hidden (script still running)")
end)

-- Status
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -40, 0, 35)
statusLabel.Position = UDim2.new(0, 20, 0, 220)
statusLabel.BackgroundTransparency = 1
statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
statusLabel.TextSize = 15
statusLabel.Font = Enum.Font.Gotham
statusLabel.Parent = MainFrame

-- ===================== MAIN LOOP =====================
RunService.RenderStepped:Connect(function()
	-- FOV Circle
	fovCircle.Visible = AIMBOT_ENABLED
	fovCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
	fovCircle.Radius = FOV
	
	-- ALWAYS-ON Aimbot (only inside FOV + on screen)
	if AIMBOT_ENABLED then
		if not isValidTarget(currentTarget) then
			currentTarget = getClosestTargetInFOV()
		end
		
		if currentTarget and currentTarget.Character and currentTarget.Character:FindFirstChild("Head") then
			local head = currentTarget.Character.Head
			Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, head.Position)
		end
	end
	
	-- ESP + Tracers + Info Box
	if ESP_ENABLED then
		for plr, draw in pairs(drawings) do
			if isValidTarget(plr) then
				local root = plr.Character:FindFirstChild("HumanoidRootPart") or plr.Character.Head
				local hum = plr.Character:FindFirstChildOfClass("Humanoid")
				local screenPos, onScreen = Camera:WorldToViewportPoint(root.Position)
				
				if onScreen then
					draw.Tracer.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
					draw.Tracer.To = Vector2.new(screenPos.X, screenPos.Y)
					draw.Tracer.Visible = true
					
					local boxPos = Vector2.new(screenPos.X + 18, screenPos.Y - 52)
					draw.Box.Position = boxPos
					draw.Box.Size = Vector2.new(150, 74)
					draw.Box.Visible = true
					
					draw.Name.Text = plr.DisplayName or plr.Name
					draw.Name.Position = boxPos + Vector2.new(75, 9)
					draw.Name.Visible = true
					
					local hpPct = math.floor((hum.Health / hum.MaxHealth) * 100)
					draw.HP.Text = "HP: " .. hpPct .. "%"
					draw.HP.Position = boxPos + Vector2.new(75, 29)
					draw.HP.Visible = true
					
					local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
					local dist = myRoot and math.floor((myRoot.Position - root.Position).Magnitude) or 0
					draw.Distance.Text = "Dist: " .. dist
					draw.Distance.Position = boxPos + Vector2.new(75, 49)
					draw.Distance.Visible = true
				else
					draw.Tracer.Visible = false
					draw.Box.Visible = false
					draw.Name.Visible = false
					draw.HP.Visible = false
					draw.Distance.Visible = false
				end
			else
				draw.Tracer.Visible = false
				draw.Box.Visible = false
				draw.Name.Visible = false
				draw.HP.Visible = false
				draw.Distance.Visible = false
			end
		end
	else
		for _, draw in pairs(drawings) do
			draw.Tracer.Visible = false
			draw.Box.Visible = false
			draw.Name.Visible = false
			draw.HP.Visible = false
			draw.Distance.Visible = false
		end
	end
	
	-- Update status
	statusLabel.Text = string.format("Aimbot: %s | ESP: %s | FOV: %d | Target: %s",
		AIMBOT_ENABLED and "ON" or "OFF",
		ESP_ENABLED and "ON" or "OFF",
		FOV,
		currentTarget and currentTarget.Name or "None")
end)

-- ===================== KEYBINDS =====================
UserInputService.InputBegan:Connect(function(input)
	if input.KeyCode == TOGGLE_CHEAT_KEY then
		AIMBOT_ENABLED = not AIMBOT_ENABLED
		ESP_ENABLED = AIMBOT_ENABLED
		print("Camron Script: " .. (AIMBOT_ENABLED and "ENABLED" or "DISABLED"))
	elseif input.KeyCode == TOGGLE_GUI_KEY then
		ScreenGui.Enabled = not ScreenGui.Enabled
	end
end)

print("✅ Camron Script loaded successfully!")
print("• Hold nothing — aimbot is always on")
• Only aims at heads INSIDE your FOV radius
• GUI is now visible (draggable)
• Press INSERT to toggle | RightShift to hide/show GUI